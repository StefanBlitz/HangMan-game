<UserControl x:Class="Hangman.Views.StatisticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="LightPink">
        <StackPanel Margin="40">
            <TextBlock Text="Global Statistics" FontSize="28" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center"/>
            <DataGrid ItemsSource="{Binding Statistics}" 
                      AutoGenerateColumns="False" 
                      IsReadOnly="True" 
                      Height="350" 
                      Background="White"
                      AlternatingRowBackground="LightGray">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="User" Binding="{Binding UserName}" Width="*"/>
                    <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="*"/>
                    <DataGridTextColumn Header="Played" Binding="{Binding GamesPlayed}" Width="80"/>
                    <DataGridTextColumn Header="Won" Binding="{Binding GamesWon}" Width="80"/>
                </DataGrid.Columns>
            </DataGrid>
            <Button Content="Return to Menu" Command="{Binding BackCommand}" Width="150" Height="40" Margin="0,30,0,0" FontSize="16"/>
        </StackPanel>
    </Grid>
</UserControl>