<UserControl x:Class="Hangman.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="LightCoral">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Welcome to Hangman! Enjoy this game!" FontSize="26" FontWeight="Bold" Margin="0,0,0,30" HorizontalAlignment="Center"/>

            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="250"/>
                    <ColumnDefinition Width="350"/>
                </Grid.ColumnDefinitions>

                <ListBox Grid.Column="0" ItemsSource="{Binding Users}" SelectedItem="{Binding SelectedUser}" DisplayMemberPath="Name" Height="250" Margin="10"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <Button Content="&lt;&lt;" Command="{Binding PreviousUserCommand}" Width="40" Height="40" Margin="5"/>
                    <Border BorderBrush="Black" BorderThickness="1" Margin="10" Width="200" Height="200">
                        <Image Source="{Binding SelectedUser.ImagePath}" Stretch="UniformToFill"/>
                    </Border>
                    <Button Content="&gt;&gt;" Command="{Binding NextUserCommand}" Width="40" Height="40" Margin="5"/>
                </StackPanel>
            </Grid>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBox Text="{Binding NewUserName, UpdateSourceTrigger=PropertyChanged}" Width="150" Height="30" VerticalContentAlignment="Center" Margin="5"/>
                <Button Content="New User" Command="{Binding NewUserCommand}" Width="90" Height="30" Margin="5"/>
                <Button Content="Delete User" Command="{Binding DeleteUserCommand}" Width="90" Height="30" Margin="5"/>
                <Button Content="Play" Command="{Binding PlayCommand}" Width="90" Height="30" Margin="5" FontWeight="Bold"/>
                <Button Content="Exit" Command="{Binding ExitCommand}" Width="90" Height="30" Margin="5" Background="DarkRed" Foreground="White"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>